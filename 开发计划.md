# 开发计划

本文记录项目待开发事项。未完成事项放在 `TODO` 章节；每条事项使用固定序号标记，方便后续讨论时明确引用。事项完成后，在对应条目前增加 `[DONE]` tag，并保留原序号与原条目，便于追踪。

## 开发规则

- 开发必须尽量保持向后兼容，不影响原有功能、原有接口语义和既有业务逻辑。
- 涉及接口、collector、provider、agent 或策略逻辑变化时，需要在 `tests/` 中补充或更新对应测试脚本进行验证。
- 涉及查询接口变化时，开发完成后必须同步更新 `cognition/查询接口说明文档.md` 与 `skills/panwatch-query-interfaces/references/query-interface-guide.md`。
- 修改完成后应按影响范围运行对应测试；若无法运行，需要在交付说明中明确原因和残余风险。

## TODO

1. [DONE] 个股历史资金流接口
   - 目标：支持按股票查询日级历史资金流入/流出序列。
   - 建议接口：`GET /api/capital-flow/{symbol}/history?market=CN&days=60`。
   - 数据来源：复用现有 `CapitalFlowCollector` 的东方财富 `fflow/daykline/get` 返回数据。
   - 实现要点：新增 `get_capital_flow_history()` 解析完整 `klines`，返回逐日主力净流入、主力占比、超大单/大单/中单/小单净流入、收盘价、涨跌幅、成交量、成交额等字段。
   - 接入范围：后端查询 API、必要的类型定义/测试、查询接口说明文档与 `panwatch-query-interfaces` skill reference 同步更新。

2. 板块/概念资金流分析 Agent
   - 目标：支持每日按行业板块、概念板块和自定义聚类规则统计资金流入/流出，为后续 Agent 分析和页面展示提供统一数据。
   - 当前缺口：现有 `/api/discovery/boards` 只返回板块涨跌幅、涨跌额和成交额；`/api/discovery/boards/{board_code}/stocks` 只返回成分股行情排行；现有 `CapitalFlowCollector` 只支持个股资金流摘要，不支持板块/概念级资金流、历史序列或自定义聚类。
   - 接口扩展子项：
     1. [DONE] 板块/概念资金流排行接口：`GET /api/sector-flows?market=CN&type=industry|concept&mode=main_net|turnover&limit=100`，返回板块/概念代码、名称、主力净流入、主力净流入占比、超大单/大单/中单/小单净额、成交额、涨跌幅、更新时间。
     2. [DONE] 单板块/概念历史资金流接口：`GET /api/sector-flows/{board_code}/history?type=industry|concept&days=60`，返回日级资金流序列，用于判断持续流入、持续流出和资金拐点。
     3. [DONE] 板块/概念成分股资金流接口：`GET /api/sector-flows/{board_code}/stocks?type=industry|concept&limit=100`，返回成分股资金流贡献度，支持解释板块资金由哪些个股驱动。
     4. [DONE] 板块/概念元数据与搜索接口：`GET /api/sectors?type=industry|concept&keyword=`，统一行业/概念代码、名称、来源、是否启用、更新时间，避免 Agent 和页面直接依赖热门榜接口。
     5. [DONE] 板块聚类规则配置接口：`GET /api/sector-flow-groups`、`PUT /api/sector-flow-groups`，维护自定义聚类规则，包括组名、包含/排除的板块或概念、权重、别名和排序。
     6. [DONE] 聚类后资金流汇总接口：`GET /api/sector-flow-groups/summary?date=YYYY-MM-DD`，按配置聚合板块/概念资金流，返回组级净流入、流入占比、贡献板块、异常变化和更新时间。
     7. [DONE] 板块资金流快照/归档接口：`POST /api/sector-flows/snapshots/refresh`、`GET /api/sector-flows/snapshots?date=YYYY-MM-DD`，供每日 Agent 复盘使用，避免重复拉取外部数据。
   - 后续开发子项（按依赖顺序）：
     8. [DONE] 聚类规则数据模型完善：确认规则是否只用本地 JSON，还是迁移到数据库；补充默认聚类模板、结构化 selector、启停状态、颜色、配置预览和配置校验。
     9. [DONE] 聚类规则配置页面：支持新增/编辑/删除聚类组，搜索点选行业/概念，配置包含/排除板块、别名、权重、颜色、排序和启停状态，并支持预览诊断、恢复默认和保存后刷新页面。
     10. [TODO] 引入 akshare/efinance 板块资金数据源适配层：优先评估并接入 `akshare`，因为当前本地版本已暴露行业/概念名称、行业/概念资金流排行、今日/3/5/10 日排行、历史资金流等函数；`efinance` 暂作为候选/备用，先确认其当前版本是否覆盖板块资金流。实现时新增 provider 抽象，保留现有 EastMoney 直连 collector 作为 fallback，不破坏当前 `/api/sector-flows/*` 语义。
         - 候选 akshare 函数：`stock_board_industry_name_em()`、`stock_board_concept_name_em()`、`stock_fund_flow_industry(symbol="即时|3日排行|5日排行|10日排行|20日排行")`、`stock_fund_flow_concept(symbol="即时|3日排行|5日排行|10日排行|20日排行")`、`stock_sector_fund_flow_rank(indicator="今日|5日|10日", sector_type="行业资金流|概念资金流")`、`stock_sector_fund_flow_hist(symbol=...)`、`stock_concept_fund_flow_hist(symbol=...)`、`stock_sector_fund_flow_summary(symbol=..., indicator="今日|5日|10日")`。
     11. [TODO] 全量行业/概念目录本地归档：新增本地目录文件或表，例如 `DATA_DIR/sector_catalog/industry.json`、`DATA_DIR/sector_catalog/concept.json`，一次性拉取并固定全部行业/概念代码、名称、类型、来源、更新时间、别名和启用状态；`/api/sectors` 搜索优先读取该目录，不再依赖资金快照里的 TopN 板块。
         - 建议接口：`POST /api/sectors/catalog/refresh?provider=akshare&type=industry|concept|all`，拉取并保存全量目录。
         - 建议接口：`GET /api/sectors/catalog?type=industry|concept&keyword=&enabled=true`，读取本地可查询目录，供聚类配置页面搜索点选。
         - 兼容处理：`GET /api/sectors?type=industry|concept&keyword=` 后续改为读本地 catalog，实时源和快照只作为缺失时 fallback。
     12. [TODO] 全量行业/概念资金快照重构：当前快照依赖东财 clist `limit=500` 和实时可用性，实际只固化资金排行返回内容。需要新增 akshare 全量快照采集，按行业、概念分别保存今日资金流，确保搜索和聚类不再只覆盖当日 TopN。
         - 建议接口：扩展 `POST /api/sector-flows/snapshots/refresh` 支持 `provider=akshare|eastmoney`、`type=industry|concept|all`、`period=today|3d|5d|10d|20d`、`force=true`。
         - 建议快照结构：按 `date/type/period/provider` 分文件保存，例如 `DATA_DIR/sector_flow_snapshots/2026-07-15/industry_today.json`，避免今日和多日排行互相覆盖。
     13. [TODO] 多周期行业/概念资金流排行接口：补充今日、3 日、5 日、10 日资金流统一查询，页面和 Agent 不再自己拼多个源。
         - 建议接口：`GET /api/sector-flows/rank?type=industry|concept&period=today|3d|5d|10d|20d&provider=akshare&refresh=false`。
         - 返回字段：板块代码、名称、类型、周期、主力净流入、主力净流入占比、涨跌幅、成交额、排名、来源、更新时间；不同数据源字段需统一映射。
         - akshare 口径：`stock_fund_flow_industry/stock_fund_flow_concept` 支持 `即时/3日排行/5日排行/10日排行/20日排行`；`stock_sector_fund_flow_rank` 支持 `今日/5日/10日`，实现时需确认两者字段差异和优先级。
     14. [TODO] 单板块多周期与历史资金流接口增强：在现有 `GET /api/sector-flows/{board_code}/history` 基础上补充按行业/概念名称或代码查询历史资金流，并支持近 3/5/10 日聚合指标。
         - 建议接口：`GET /api/sector-flows/{board_code}/periods?type=industry|concept&periods=3,5,10`，返回多周期净流入、连续流入天数、净流入强度和趋势方向。
         - 建议接口增强：`GET /api/sector-flows/{board_code}/history?type=industry|concept&provider=akshare&days=60`，用 akshare 历史函数补齐东财直连失败场景。
     15. [TODO] 资金聚类汇总改用本地 catalog + 多周期快照：聚类规则中的 selector 必须从全量 catalog 匹配，组级汇总支持 `period=today|3d|5d|10d`，并返回各周期净流入、强度、排名变化和贡献板块。
         - 建议接口：扩展 `GET /api/sector-flow-groups/summary?date=YYYY-MM-DD&period=today|3d|5d|10d`。
         - 建议接口：扩展 `POST /api/sector-flow-groups/preview?period=today|3d|5d|10d`，配置预览时可选择资金周期。
     16. [TODO] 数据快照与复盘留痕：确认快照保留周期、手动刷新入口、失败重试、盘后自动固化、历史对比展示和数据源标识，避免每日报告重复拉外部数据。
     17. [TODO] 板块资金流指标体系：定义 Agent 使用的核心指标，包括当日主力净流入、近 3/5/10 日趋势、连续流入天数、流入强度、成交额占比、贡献板块/个股、异常放量、排名变化和资金拐点。
     18. [TODO] 板块资金流评分规则：设计可解释的打分模型，输出强流入、温和流入、分歧、流出、风险释放等状态，并支持后续配置阈值。
     19. [TODO] 板块资金流页面升级：在 `mozz 分析` 页面继续扩展聚类汇总、周期切换、板块排行、历史趋势、成分股贡献、快照状态和 Agent 报告入口。
         - [DONE] 首个窗格：新增 `mozz 分析` 导航页，完成资金聚类汇总、聚类排行、贡献板块、快照刷新展示和 1/3/5/10 分钟自动刷新设置，默认 5 分钟。
         - [TODO] 聚类配置搜索改为调用本地全量 catalog；页面增加周期切换 `今日/3日/5日/10日`，并展示数据源、快照时间、是否来自本地缓存/实时刷新，避免用户误判“搜不到=没有该板块”。
     20. [TODO] 报告结构与 Prompt：新增 prompt 模板，固定输出市场总览、流入主线、流出风险、聚类变化、成分股驱动、次日观察清单。
     21. [TODO] 每日板块资金流 Agent：新增独立 Agent，读取快照、聚类规则、历史资金流和成分股贡献，生成每日板块资金流分析报告。
     22. [TODO] Agent 注册与调度入口：在 `server.py` 注册新 Agent，补充默认配置、手动触发接口和可选定时任务，避免影响现有 Agent。
     23. [TODO] 测试与文档收口：为新增 provider、catalog refresh、rank、history、group summary、页面 API、Agent、评分规则和调度补充测试；完成后同步更新 `cognition/查询接口说明文档.md`、`skills/panwatch-query-interfaces/references/query-interface-guide.md` 和用户使用说明。
